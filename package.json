{
  "name": "telier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:rebuild": "npm run build --prefix frontend-v2",
    "lint:rebuild": "npm run lint --prefix frontend-v2",
    "screenshots:crawl": "node capture.js",
    "smoke": "node smoke-rebuild.js",
    "smoke:rebuild": "node smoke-rebuild.js",
    "smoke:rebuild:scale": "node smoke-rebuild-scale.js"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "wrangler": "^4.77.0"
  }
}
