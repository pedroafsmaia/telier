{
  "name": "telier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --outfile=src/app.bundle.js --target=es2020",
    "watch": "esbuild src/app.js --bundle --outfile=src/app.bundle.js --target=es2020 --watch",
    "dev": "npm run watch",
    "screenshots": "node capture.js",
    "smoke": "node smoke.js"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "wrangler": "^4.76.0"
  }
}
