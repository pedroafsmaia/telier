// Telier App Entry Point
// This file will be bundled by esbuild to create src/app.bundle.js

console.log('Telier app starting...');

// TODO: Import all modules here during FASE 3.2
// For now, we'll keep the monolithic script in index.html

// Temporary placeholder - will be replaced during FASE 3.2
if (typeof window !== 'undefined') {
  window.appReady = true;
}
